Discover the Meteor project path.

Usage:
``` js
import meteorProjectPath from 'meteor-project-path';
console.log('meteorProjectPath'); // outputs something like /path/to/Meteor/project or on Windows: D:\path\to\Meteor\project
```
